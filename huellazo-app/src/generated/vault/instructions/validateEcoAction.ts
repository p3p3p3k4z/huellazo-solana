/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { HUELLAZO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const VALIDATE_ECO_ACTION_DISCRIMINATOR = new Uint8Array([
  87, 240, 133, 131, 143, 141, 158, 87,
]);

export function getValidateEcoActionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VALIDATE_ECO_ACTION_DISCRIMINATOR,
  );
}

export type ValidateEcoActionInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountPassport extends string | AccountMeta<string> = string,
  TAccountMerchant extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPassport extends string
        ? WritableAccount<TAccountPassport>
        : TAccountPassport,
      TAccountMerchant extends string
        ? ReadonlyAccount<TAccountMerchant>
        : TAccountMerchant,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ValidateEcoActionInstructionData = {
  discriminator: ReadonlyUint8Array;
  actionId: number;
};

export type ValidateEcoActionInstructionDataArgs = { actionId: number };

export function getValidateEcoActionInstructionDataEncoder(): FixedSizeEncoder<ValidateEcoActionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["actionId", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VALIDATE_ECO_ACTION_DISCRIMINATOR }),
  );
}

export function getValidateEcoActionInstructionDataDecoder(): FixedSizeDecoder<ValidateEcoActionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["actionId", getU8Decoder()],
  ]);
}

export function getValidateEcoActionInstructionDataCodec(): FixedSizeCodec<
  ValidateEcoActionInstructionDataArgs,
  ValidateEcoActionInstructionData
> {
  return combineCodec(
    getValidateEcoActionInstructionDataEncoder(),
    getValidateEcoActionInstructionDataDecoder(),
  );
}

export type ValidateEcoActionInput<
  TAccountPassport extends string = string,
  TAccountMerchant extends string = string,
  TAccountAuthority extends string = string,
> = {
  passport: Address<TAccountPassport>;
  merchant: Address<TAccountMerchant>;
  authority: TransactionSigner<TAccountAuthority>;
  actionId: ValidateEcoActionInstructionDataArgs["actionId"];
};

export function getValidateEcoActionInstruction<
  TAccountPassport extends string,
  TAccountMerchant extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof HUELLAZO_PROGRAM_ADDRESS,
>(
  input: ValidateEcoActionInput<
    TAccountPassport,
    TAccountMerchant,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): ValidateEcoActionInstruction<
  TProgramAddress,
  TAccountPassport,
  TAccountMerchant,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? HUELLAZO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    passport: { value: input.passport ?? null, isWritable: true },
    merchant: { value: input.merchant ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.passport),
      getAccountMeta(accounts.merchant),
      getAccountMeta(accounts.authority),
    ],
    data: getValidateEcoActionInstructionDataEncoder().encode(
      args as ValidateEcoActionInstructionDataArgs,
    ),
    programAddress,
  } as ValidateEcoActionInstruction<
    TProgramAddress,
    TAccountPassport,
    TAccountMerchant,
    TAccountAuthority
  >);
}

export type ParsedValidateEcoActionInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    passport: TAccountMetas[0];
    merchant: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: ValidateEcoActionInstructionData;
};

export function parseValidateEcoActionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedValidateEcoActionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      passport: getNextAccount(),
      merchant: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getValidateEcoActionInstructionDataDecoder().decode(instruction.data),
  };
}
