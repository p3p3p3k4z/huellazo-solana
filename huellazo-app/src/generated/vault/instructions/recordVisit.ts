/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { HUELLAZO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const RECORD_VISIT_DISCRIMINATOR = new Uint8Array([
  194, 185, 156, 151, 246, 173, 212, 214,
]);

export function getRecordVisitDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_VISIT_DISCRIMINATOR,
  );
}

export type RecordVisitInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountPassport extends string | AccountMeta<string> = string,
  TAccountMerchant extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPassport extends string
        ? WritableAccount<TAccountPassport>
        : TAccountPassport,
      TAccountMerchant extends string
        ? ReadonlyAccount<TAccountMerchant>
        : TAccountMerchant,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type RecordVisitInstructionData = {
  discriminator: ReadonlyUint8Array;
  xp: bigint;
  points: bigint;
};

export type RecordVisitInstructionDataArgs = {
  xp: number | bigint;
  points: number | bigint;
};

export function getRecordVisitInstructionDataEncoder(): FixedSizeEncoder<RecordVisitInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["xp", getU64Encoder()],
      ["points", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RECORD_VISIT_DISCRIMINATOR }),
  );
}

export function getRecordVisitInstructionDataDecoder(): FixedSizeDecoder<RecordVisitInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["xp", getU64Decoder()],
    ["points", getU64Decoder()],
  ]);
}

export function getRecordVisitInstructionDataCodec(): FixedSizeCodec<
  RecordVisitInstructionDataArgs,
  RecordVisitInstructionData
> {
  return combineCodec(
    getRecordVisitInstructionDataEncoder(),
    getRecordVisitInstructionDataDecoder(),
  );
}

export type RecordVisitInput<
  TAccountPassport extends string = string,
  TAccountMerchant extends string = string,
  TAccountAuthority extends string = string,
> = {
  passport: Address<TAccountPassport>;
  merchant: Address<TAccountMerchant>;
  authority: TransactionSigner<TAccountAuthority>;
  xp: RecordVisitInstructionDataArgs["xp"];
  points: RecordVisitInstructionDataArgs["points"];
};

export function getRecordVisitInstruction<
  TAccountPassport extends string,
  TAccountMerchant extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof HUELLAZO_PROGRAM_ADDRESS,
>(
  input: RecordVisitInput<
    TAccountPassport,
    TAccountMerchant,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): RecordVisitInstruction<
  TProgramAddress,
  TAccountPassport,
  TAccountMerchant,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? HUELLAZO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    passport: { value: input.passport ?? null, isWritable: true },
    merchant: { value: input.merchant ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.passport),
      getAccountMeta(accounts.merchant),
      getAccountMeta(accounts.authority),
    ],
    data: getRecordVisitInstructionDataEncoder().encode(
      args as RecordVisitInstructionDataArgs,
    ),
    programAddress,
  } as RecordVisitInstruction<
    TProgramAddress,
    TAccountPassport,
    TAccountMerchant,
    TAccountAuthority
  >);
}

export type ParsedRecordVisitInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    passport: TAccountMetas[0];
    merchant: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: RecordVisitInstructionData;
};

export function parseRecordVisitInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordVisitInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      passport: getNextAccount(),
      merchant: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getRecordVisitInstructionDataDecoder().decode(instruction.data),
  };
}
