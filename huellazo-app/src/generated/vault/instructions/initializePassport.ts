/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { HUELLAZO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_PASSPORT_DISCRIMINATOR = new Uint8Array([
  61, 77, 198, 139, 101, 90, 68, 137,
]);

export function getInitializePassportDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PASSPORT_DISCRIMINATOR,
  );
}

export type InitializePassportInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountPassport extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPassport extends string
        ? WritableAccount<TAccountPassport>
        : TAccountPassport,
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializePassportInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializePassportInstructionDataArgs = {};

export function getInitializePassportInstructionDataEncoder(): FixedSizeEncoder<InitializePassportInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INITIALIZE_PASSPORT_DISCRIMINATOR }),
  );
}

export function getInitializePassportInstructionDataDecoder(): FixedSizeDecoder<InitializePassportInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializePassportInstructionDataCodec(): FixedSizeCodec<
  InitializePassportInstructionDataArgs,
  InitializePassportInstructionData
> {
  return combineCodec(
    getInitializePassportInstructionDataEncoder(),
    getInitializePassportInstructionDataDecoder(),
  );
}

export type InitializePassportInput<
  TAccountPassport extends string = string,
  TAccountUser extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  passport: Address<TAccountPassport>;
  user: TransactionSigner<TAccountUser>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializePassportInstruction<
  TAccountPassport extends string,
  TAccountUser extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HUELLAZO_PROGRAM_ADDRESS,
>(
  input: InitializePassportInput<
    TAccountPassport,
    TAccountUser,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializePassportInstruction<
  TProgramAddress,
  TAccountPassport,
  TAccountUser,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HUELLAZO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    passport: { value: input.passport ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.passport),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializePassportInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializePassportInstruction<
    TProgramAddress,
    TAccountPassport,
    TAccountUser,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type ParsedInitializePassportInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    passport: TAccountMetas[0];
    user: TAccountMetas[1];
    config: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitializePassportInstructionData;
};

export function parseInitializePassportInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializePassportInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      passport: getNextAccount(),
      user: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializePassportInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
