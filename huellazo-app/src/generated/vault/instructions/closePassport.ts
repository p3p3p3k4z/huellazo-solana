/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { HUELLAZO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CLOSE_PASSPORT_DISCRIMINATOR = new Uint8Array([
  198, 21, 251, 56, 20, 59, 143, 23,
]);

export function getClosePassportDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_PASSPORT_DISCRIMINATOR,
  );
}

export type ClosePassportInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountPassport extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPassport extends string
        ? WritableAccount<TAccountPassport>
        : TAccountPassport,
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      ...TRemainingAccounts,
    ]
  >;

export type ClosePassportInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClosePassportInstructionDataArgs = {};

export function getClosePassportInstructionDataEncoder(): FixedSizeEncoder<ClosePassportInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_PASSPORT_DISCRIMINATOR }),
  );
}

export function getClosePassportInstructionDataDecoder(): FixedSizeDecoder<ClosePassportInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClosePassportInstructionDataCodec(): FixedSizeCodec<
  ClosePassportInstructionDataArgs,
  ClosePassportInstructionData
> {
  return combineCodec(
    getClosePassportInstructionDataEncoder(),
    getClosePassportInstructionDataDecoder(),
  );
}

export type ClosePassportInput<
  TAccountPassport extends string = string,
  TAccountUser extends string = string,
> = {
  passport: Address<TAccountPassport>;
  user: TransactionSigner<TAccountUser>;
};

export function getClosePassportInstruction<
  TAccountPassport extends string,
  TAccountUser extends string,
  TProgramAddress extends Address = typeof HUELLAZO_PROGRAM_ADDRESS,
>(
  input: ClosePassportInput<TAccountPassport, TAccountUser>,
  config?: { programAddress?: TProgramAddress },
): ClosePassportInstruction<TProgramAddress, TAccountPassport, TAccountUser> {
  // Program address.
  const programAddress = config?.programAddress ?? HUELLAZO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    passport: { value: input.passport ?? null, isWritable: true },
    user: { value: input.user ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.passport),
      getAccountMeta(accounts.user),
    ],
    data: getClosePassportInstructionDataEncoder().encode({}),
    programAddress,
  } as ClosePassportInstruction<
    TProgramAddress,
    TAccountPassport,
    TAccountUser
  >);
}

export type ParsedClosePassportInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    passport: TAccountMetas[0];
    user: TAccountMetas[1];
  };
  data: ClosePassportInstructionData;
};

export function parseClosePassportInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClosePassportInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { passport: getNextAccount(), user: getNextAccount() },
    data: getClosePassportInstructionDataDecoder().decode(instruction.data),
  };
}
