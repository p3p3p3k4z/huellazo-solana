/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { HUELLAZO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const UPDATE_MERCHANT_DISCRIMINATOR = new Uint8Array([
  192, 114, 143, 220, 199, 50, 234, 165,
]);

export function getUpdateMerchantDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_MERCHANT_DISCRIMINATOR,
  );
}

export type UpdateMerchantInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountMerchant extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMerchant extends string
        ? WritableAccount<TAccountMerchant>
        : TAccountMerchant,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMerchantInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: Option<string>;
  active: Option<boolean>;
};

export type UpdateMerchantInstructionDataArgs = {
  name: OptionOrNullable<string>;
  active: OptionOrNullable<boolean>;
};

export function getUpdateMerchantInstructionDataEncoder(): Encoder<UpdateMerchantInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "name",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      ["active", getOptionEncoder(getBooleanEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_MERCHANT_DISCRIMINATOR }),
  );
}

export function getUpdateMerchantInstructionDataDecoder(): Decoder<UpdateMerchantInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "name",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    ["active", getOptionDecoder(getBooleanDecoder())],
  ]);
}

export function getUpdateMerchantInstructionDataCodec(): Codec<
  UpdateMerchantInstructionDataArgs,
  UpdateMerchantInstructionData
> {
  return combineCodec(
    getUpdateMerchantInstructionDataEncoder(),
    getUpdateMerchantInstructionDataDecoder(),
  );
}

export type UpdateMerchantInput<
  TAccountMerchant extends string = string,
  TAccountAuthority extends string = string,
> = {
  merchant: Address<TAccountMerchant>;
  authority: TransactionSigner<TAccountAuthority>;
  name: UpdateMerchantInstructionDataArgs["name"];
  active: UpdateMerchantInstructionDataArgs["active"];
};

export function getUpdateMerchantInstruction<
  TAccountMerchant extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof HUELLAZO_PROGRAM_ADDRESS,
>(
  input: UpdateMerchantInput<TAccountMerchant, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateMerchantInstruction<
  TProgramAddress,
  TAccountMerchant,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? HUELLAZO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    merchant: { value: input.merchant ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.merchant),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateMerchantInstructionDataEncoder().encode(
      args as UpdateMerchantInstructionDataArgs,
    ),
    programAddress,
  } as UpdateMerchantInstruction<
    TProgramAddress,
    TAccountMerchant,
    TAccountAuthority
  >);
}

export type ParsedUpdateMerchantInstruction<
  TProgram extends string = typeof HUELLAZO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    merchant: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateMerchantInstructionData;
};

export function parseUpdateMerchantInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateMerchantInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { merchant: getNextAccount(), authority: getNextAccount() },
    data: getUpdateMerchantInstructionDataDecoder().decode(instruction.data),
  };
}
