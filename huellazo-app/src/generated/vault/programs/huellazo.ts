/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  type Address,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  parseClosePassportInstruction,
  parseInitializeConfigInstruction,
  parseInitializePassportInstruction,
  parseRecordVisitInstruction,
  parseRegisterMerchantInstruction,
  parseUpdateMerchantInstruction,
  parseValidateEcoActionInstruction,
  type ParsedClosePassportInstruction,
  type ParsedInitializeConfigInstruction,
  type ParsedInitializePassportInstruction,
  type ParsedRecordVisitInstruction,
  type ParsedRegisterMerchantInstruction,
  type ParsedUpdateMerchantInstruction,
  type ParsedValidateEcoActionInstruction,
} from "../instructions";

export const HUELLAZO_PROGRAM_ADDRESS = "" as Address<"">;

export enum HuellazoAccount {
  GlobalConfig,
  Passport,
  Merchant,
}

export function identifyHuellazoAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): HuellazoAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([149, 8, 156, 202, 160, 252, 176, 217]),
      ),
      0,
    )
  ) {
    return HuellazoAccount.GlobalConfig;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([18, 61, 245, 239, 6, 15, 18, 34]),
      ),
      0,
    )
  ) {
    return HuellazoAccount.Passport;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([71, 235, 30, 40, 231, 21, 32, 64]),
      ),
      0,
    )
  ) {
    return HuellazoAccount.Merchant;
  }
  throw new Error(
    "The provided account could not be identified as a huellazo account.",
  );
}

export enum HuellazoInstruction {
  InitializeConfig,
  InitializePassport,
  RegisterMerchant,
  RecordVisit,
  ValidateEcoAction,
  UpdateMerchant,
  ClosePassport,
}

export function identifyHuellazoInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): HuellazoInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([208, 127, 21, 1, 194, 190, 196, 70]),
      ),
      0,
    )
  ) {
    return HuellazoInstruction.InitializeConfig;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([61, 77, 198, 139, 101, 90, 68, 137]),
      ),
      0,
    )
  ) {
    return HuellazoInstruction.InitializePassport;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([238, 245, 77, 132, 161, 88, 216, 248]),
      ),
      0,
    )
  ) {
    return HuellazoInstruction.RegisterMerchant;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([194, 185, 156, 151, 246, 173, 212, 214]),
      ),
      0,
    )
  ) {
    return HuellazoInstruction.RecordVisit;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([87, 240, 133, 131, 143, 141, 158, 87]),
      ),
      0,
    )
  ) {
    return HuellazoInstruction.ValidateEcoAction;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([192, 114, 143, 220, 199, 50, 234, 165]),
      ),
      0,
    )
  ) {
    return HuellazoInstruction.UpdateMerchant;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([198, 21, 251, 56, 20, 59, 143, 23]),
      ),
      0,
    )
  ) {
    return HuellazoInstruction.ClosePassport;
  }
  throw new Error(
    "The provided instruction could not be identified as a huellazo instruction.",
  );
}

export type ParsedHuellazoInstruction<TProgram extends string = ""> =
  | ({
      instructionType: HuellazoInstruction.InitializeConfig;
    } & ParsedInitializeConfigInstruction<TProgram>)
  | ({
      instructionType: HuellazoInstruction.InitializePassport;
    } & ParsedInitializePassportInstruction<TProgram>)
  | ({
      instructionType: HuellazoInstruction.RegisterMerchant;
    } & ParsedRegisterMerchantInstruction<TProgram>)
  | ({
      instructionType: HuellazoInstruction.RecordVisit;
    } & ParsedRecordVisitInstruction<TProgram>)
  | ({
      instructionType: HuellazoInstruction.ValidateEcoAction;
    } & ParsedValidateEcoActionInstruction<TProgram>)
  | ({
      instructionType: HuellazoInstruction.UpdateMerchant;
    } & ParsedUpdateMerchantInstruction<TProgram>)
  | ({
      instructionType: HuellazoInstruction.ClosePassport;
    } & ParsedClosePassportInstruction<TProgram>);

export function parseHuellazoInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedHuellazoInstruction<TProgram> {
  const instructionType = identifyHuellazoInstruction(instruction);
  switch (instructionType) {
    case HuellazoInstruction.InitializeConfig: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HuellazoInstruction.InitializeConfig,
        ...parseInitializeConfigInstruction(instruction),
      };
    }
    case HuellazoInstruction.InitializePassport: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HuellazoInstruction.InitializePassport,
        ...parseInitializePassportInstruction(instruction),
      };
    }
    case HuellazoInstruction.RegisterMerchant: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HuellazoInstruction.RegisterMerchant,
        ...parseRegisterMerchantInstruction(instruction),
      };
    }
    case HuellazoInstruction.RecordVisit: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HuellazoInstruction.RecordVisit,
        ...parseRecordVisitInstruction(instruction),
      };
    }
    case HuellazoInstruction.ValidateEcoAction: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HuellazoInstruction.ValidateEcoAction,
        ...parseValidateEcoActionInstruction(instruction),
      };
    }
    case HuellazoInstruction.UpdateMerchant: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HuellazoInstruction.UpdateMerchant,
        ...parseUpdateMerchantInstruction(instruction),
      };
    }
    case HuellazoInstruction.ClosePassport: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: HuellazoInstruction.ClosePassport,
        ...parseClosePassportInstruction(instruction),
      };
    }
    default:
      throw new Error(
        `Unrecognized instruction type: ${instructionType as string}`,
      );
  }
}
